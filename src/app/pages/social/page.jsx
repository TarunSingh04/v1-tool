import Social from "@/app/components/page-components/social/Social";
import { Suspense } from "react";

export default function SocialPage() {
  return (
   <Suspense>
    <Social />
   </Suspense>
  );
}